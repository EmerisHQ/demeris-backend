apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "chart.labels" . | indent 4 }}
type: Opaque
data:
  coinmarketcapKey: {{ .Values.coinmarketcapKey | b64enc }}
  fixerKey: {{ .Values.fixerKey | b64enc }}